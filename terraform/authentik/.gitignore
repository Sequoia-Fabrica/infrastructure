# Ephemeral: regenerated by setup-env.sh each session
imports.generated.tf

# Never persist state or plans (they contain the instance's live config)
*.tfstate
*.tfstate.*
tfplan
*.tfplan
.terraform/
*.tfvars
